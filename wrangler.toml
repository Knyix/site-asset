name = "baronfear"
compatibility_date = "2026-06-21"

# Assets-only Worker: serves the static files in ./public with no Worker script.
# Deploy with `wrangler deploy` (not `wrangler pages deploy`).
[assets]
directory = "./public"
